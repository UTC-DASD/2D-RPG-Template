using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColorBlindOption : MonoBehaviour
{
    void Start()
    {
                //gameObj = GameObject.Find("Good Guy");

    }

    // Update is called once per frame
    /*void Update()
    {
        if(MaterialChanger.isCool == true)
        {
        foreach(GameObject gameObj in GameObject.FindObjectsOfType<GameObject>())
        {
            if(gameObj.name == "Bad Guy")
            {
                gameObj.GetComponent<Renderer>();
                Color customColor = new Color(0.4f, 0.9f, 200f, 1.0f);
                cubeRenderer.material.SetColor("_Color", customColor);
            }
        }
        }
    }*/
}
